const INITIAL_STATE = {};

const login = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    //TODO: Add cases when needed
    default:
      return state;
  }
};

export default login;
